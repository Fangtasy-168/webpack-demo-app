const parseInputs = (...input) => {
    return input.map(str => parseInt(str));
};

// function split from the original app.js file goes through the user inputs converts into string and dervies the number